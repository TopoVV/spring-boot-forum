spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/h2/databases/forum-test-db
    username: topovv
    password: admin
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format-sql: true
